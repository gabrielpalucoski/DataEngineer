import numpy as np

# Create a Numpy array
a = np.array([1, 2, 3, 4, 5])
print("Array:", a)

# Calculate the mean of the array
mean = np.mean(a)
print("Mean:", mean)

# Add 100 to each element of the array
b = a + 100
print("Array + 100:", b)
